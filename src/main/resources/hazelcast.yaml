hazelcast:
  cluster-name: 'manty'
  network:
    join:
      multicast:
        enabled: false
      tcp-ip:
        enabled: true
        member-list:
          - localhost:5701
          - localhost:5702
          - localhost:5703
          - localhost:5704
